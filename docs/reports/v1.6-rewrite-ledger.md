# Fusekit v1.6 rewrite evidence ledger

This Phase 1 audit was completed on 2026-07-19 and reverified on 2026-07-20. It
is the evidence and disposition ledger for the hard-cut rewrite. It records why
each subsystem was retained, moved, or deleted and its final implementation
disposition.

## Decision

Fusekit v1.6 is a clean protocol and architecture break. It does not preserve
old APIs, wire versions, journals, migration paths, or mixed-version behavior.
Only the exact v1 protocol suite is admitted. There is no canary or
compatibility period.

The rewrite has three ownership boundaries:

- daemonkit owns generic daemon identity, signed-peer trust, process takeover,
  crash-safe generation draining, child supervision, and bounded wire serving;
- Fusekit owns filesystem identity, content revisions, materialization,
  enumeration deltas, mutation convergence, mounts, leases, and teardown;
- consumers own domain policy, content production, and their signed app
  identity and packaging. In particular, cc-pool owns Claude-specific path
  classification and settings synthesis, but not File Provider identity,
  invalidation, retry, or lifecycle mechanics.

There is no standalone `FuseKitHolder.app`. Each consumer's fixed signed app
embeds `holder.Runtime`. `holder.NewRuntimePlan` derives a `RuntimePlan` with
distinct broker and runtime trust requirements and a daemon-facing
`DeploymentPlan`; one Mach-O may fill both roles only when those contracts are
identical. The runtime role calls `holder.RunChild` before normal application
startup. daemonkit owns signed-app launch, launchd service policy, lifecycle,
transport, and process primitives. Fusekit owns its child-mode dispatch,
filesystem modes, and the runtime composed from them.

This split is the gate for every design decision below. A mechanism needed by
two filesystem consumers belongs in Fusekit. A mechanism with no filesystem
semantics belongs in daemonkit. Consumer-side workarounds are deleted, not
renamed.

## Release boundary

| Repository | Prior release line | Hard-cut release line |
|---|---|---|
| Fusekit | `v1.5.x` | `v1.6.0` |
| daemonkit | `v0.1.x` | `v0.2.0` |
| cc-pool | `v0.59.x` | `v0.60.0` |
| cc-notes | `v0.35.x` | `v0.36.0` |
| cc-squash Go module | `v0.1.x` | `v0.2.0` |

The Fusekit `v1.5.0` release moved `proc`, `service`, `appgroup`, and `version`
onto daemonkit in `2c0efc7`. It did not rewrite mountd, content, File Provider,
leases, journals, or mount teardown. Treating v1.5 as the daemon rewrite would
preserve the exact accidental architecture this pass is intended to remove.

## History ledger

The history is not a sequence of isolated bugs. It shows the same missing
abstractions being rediscovered at progressively higher layers.

| Period | Representative commits | What failed | Rewrite conclusion |
|---|---|---|---|
| 2026-06-17 to 06-24 | `fdf43f4`, `f1a9014`, `daef594`, `f89ba35` | A test binary could re-exec as a holder and fork-storm the host; stable executable paths and process limits were added around the self-owning process model. | Keep the process-limit test wrapper. Delete self-exec and stable-copy ownership from Fusekit; run each consumer's fixed signed app under daemonkit lifecycle. |
| 2026-06-23 to 06-27 | `87e9d2c`, `3d2721b`, `2717bc1`, `589e506` | Version skew, content bridge unavailability, TCC readiness, and teardown were handled by separate probes and policies. | One typed readiness state and one generation lifecycle must cover startup, service, drain, and teardown. |
| 2026-07-01 to 07-03 | `b79c7c8`, `3978745`, `2b240bd`, `8cf683b` | Unstable synthetic attributes reached the macOS NFS panic window; attr-cache and silly-rename fixes followed; mux then introduced another identity layer. | File identity and revisions are first-class contracts. Traits are declarative and immutable for a mount generation. Mux is not a special teardown branch. |
| 2026-07-04 to 07-09 | `898cc4f`, `445a2c3`, `315e4e0`, `4ed4095`, `1290edc` | File Provider readiness, bridge ownership, offline classification, spooling, and conflict handling accreted independently. | Content state, materialization, and conflicts use one revisioned state machine. Quarantine is a typed outcome, not a log-only escape hatch. |
| 2026-07-10 to 07-12 | `a5fa142`, `94f1a81`, `be4cd60`, `99c282a`, `cbd6d59`, `2063d4c`, `14bf8fe` | Holder v2 required repeated closure passes for owner proof, lease fences, journals, signal defusal, busy/wedge classification, and carcass proof. | Preserve the proven safety invariants, not the implementation. Rebuild them as one daemonkit generation transition plus one Fusekit mount state machine. |
| 2026-07-14 | `e3125e1`, `839da8e`, `fd72dc1`, `d101757` | Fingerprint-gated File Provider signaling and bridge self-test fixed redundant work and transport/content ambiguity but remained bolt-ons to separate protocols. | Revision equality and typed health belong in the protocol core and drive delta signaling directly. |
| 2026-07-18 to 07-19 | daemonkit `a4eafe9`, `ae24230`, `bc91e42`, `4d3e9a9`; Fusekit `2c0efc7` | daemonkit established the generic lifecycle primitives, while Fusekit only flipped imports. | Compose daemonkit directly. Do not retain duplicate servers, takeover ladders, trust checks, skew handling, or drain journals in Fusekit. |

Two historical results remain non-negotiable:

1. no code path force-unmounts without the full carcass proof and an exclusive
   lease fence; a timeout or hung stat is never proof of death;
2. the signed application identity and App Group identity are deployment ABI.
   Neither may depend on a Cellar path, ad-hoc binary, copied executable, or
   temporary or versioned application path.

## Current architecture delta

### Deleted Fusekit mechanisms

- `mountd`, its protocol envelopes, holder server, journal replay, retirement,
  client/spawn surface, and feature negotiation are gone.
- The root in-process mount lifecycle, `MountSet`, `MountSpec`, mount-table
  probes, old live tests, and standalone holder binary are gone.
- `content.Source`, `Tree`, `WritableTree`, `HandleTree`, their bridge and relay,
  `holderfs`, overlay selection, and the old File Provider control runtime are
  gone.
- Per-directory lease state, process strikes, retirement breakers, versioned
  holder state, the VM stress driver, and its legacy scenario modes are gone.
- The only retained native primitives are the signed child's FUSE-T facts and
  generic AppleDouble blocking policy.

CatalogFS parity is pinned directly: opaque object/inode identity, exact
generation fences, immutable paged enumeration, open snapshots surviving
replacement, prepared mutations, transactional replace-over-target, same-tenant
serialization, and AppleDouble read/write behavior all have focused tests.

### Composed daemonkit mechanisms

The rewrite composes daemonkit takeover, drain, trust, exact persistent
sessions, process identity, reaping, and disposable workers through
`daemon.Runtime`. FuseKit adds filesystem messages and semantic scheduling but
does not wrap those mechanisms in near-duplicates. daemonkit owns listener and
shutdown ordering; FuseKit owns catalog and tenant convergence.

The reverified daemonkit baseline activates durable state only after listener
ownership, reserves capacity for protected lifecycle sessions, requires exact
authenticated application settlement, boot-fences durable process records,
drains workers before cancellation, manages disposable and long-lived child
processes, and launches signed applications with explicit arguments. Fusekit
uses `supervise.Pool` and managed `Process` mechanics; generic keep-alive
arbitration, if daemonkit regains it for another consumer, does not own
Fusekit's semantic tenant lanes.

The daemonkit v0.2 boundary supplies the composed runtime
activation/readiness contract, exact session transport, embedded-process
ownership, app stop/retirement primitives, durable process settlement, and the
single v1 runtime epoch used by the holder. FuseKit retains no local copies.

`daemonkit-is-too-you-hazy-spark.md` proposes removing daemonkit's internal
frame and lifecycle version counters in favor of the version-free `DKSF`
protocol family. That plan is not part of this release: the pinned daemonkit
boundary uses one exact v1 wire/lifecycle epoch. Fusekit catalog, mount, and
source-driver sessions independently require exact generated v1 protocol
equality and reject mismatches before mutation. Neither layer has a
compatibility reader.

## File Provider amplification ledger

The initiating `.claude.json` or `settings.json` write is real but is not the
failure. The current stack amplifies one semantic change into multi-domain
reconciliation and then extends it through document-ID churn.

| Link | Current behavior | Required invariant |
|---|---|---|
| Event fan-out | cc-pool's coordinator loops over active accounts and signals every domain, even when a domain's effective revision did not change. | The catalog commit persists each tenant's effective fingerprint and no-op decision. Fusekit emits one latest-wins invalidation only to domains whose fingerprint advanced. |
| Temporary write | The extension creates a private temporary item during an atomic write. | Temporary names and final names are aliases in one mutation transaction, not independent identities. |
| Promotion | Mutation policy removes the private temporary item and returns a computed item whose role-derived `ItemID` differs. | One object keeps its opaque File Provider identifier across private, materializing, computed, renamed, and refreshed states. Reusing a deleted path creates a new object identifier. |
| Enumeration | Each retry rebuilds the complete root and performs bridge reads. | Enumeration is revisioned and delta-based. Unchanged nodes require no content read and preserve document identifiers. |
| Retry | `itemCollision`, `ESTALE`, missing-on-disk cleanup, and delayed continuation cause macOS to retry and relaunch extensions. | Collision and stale-ID conditions are prevented by construction; retries are bounded to a revision and coalesced. |

The consumer boundary excludes global notify loops and role-derived identity
policy. A consumer provides content and classification through FuseKit and
cannot retain an alternate File Provider coordinator, identity table,
enumeration cache, or retry loop.

Affected logical keys remain bounded, paged causal-audit data. They are not the
runtime targeting index: convergence must never reconstruct tenant
effectiveness with an O(commits times affected keys) resolver after the catalog
commit. Replay reads the persisted per-tenant fingerprint and no-op decision
that were committed with the source revision.

Acceptance for the reported incident is stronger than "no crash." One base
configuration write produces at most one effective-revision advance per
affected domain, zero `itemCollision` events, zero wrong-document `ESTALE`
events, no full-root rebuild for unchanged entries, and no repeated extension
launch burst after convergence.

## Plan disposition ledger

The audit searched `~/.claude/plans` for substantive Fusekit, fuse-t, holder,
File Provider, and daemonkit references. Agent copies and research arms are
collapsed into their canonical plan. Plans that merely mention Fusekit as an
example or dependency are excluded. Consumer-only Finder naming and visibility
plans such as `these-fp-ones-are-floating-conway.md` and
`lets-prevent-all-the-velvety-whistle.md` remain consumer presentation policy,
not Fusekit requirements.

### Adopt the idea, not the old implementation

| Plan | Useful idea retained | Rewrite placement |
|---|---|---|
| `looks-like-we-broke-glowing-kahan.md` | Materialized is not fresh; readiness needs an exact authoritative content revision. | Fusekit content and File Provider protocol, excluding a widget-only XPC side channel. |
| `do-changes-in-symlink-witty-taco.md` | Atomic-replace-aware watching and latest-wins coalescing. | Fusekit revision publisher shared by symlink, mount, and File Provider views. |
| `we-should-defauly-mount-atomic-cupcake.md` | Declarative I/O traits, read-only enforcement, and stable open-handle snapshots. | The replacement source contract and mount-generation configuration. |
| `resuming-by-id-across-serialized-robin.md` | A control-plane acknowledgement is not content readiness; selection needs app-side served-content proof. | Fusekit typed readiness, consumed by cc-pool selection without consumer-specific probing. |
| `side-discovery-pooled-linear-map.md` | Conflicting regular files must be preserved in quarantine and surfaced as a typed state. | Fusekit mutation convergence across every backend. |
| `when-on-macos-fusekit-cc-pool-hazy-rabin.md` | A fixed, signed, multi-tenant holder structurally eliminates test-binary self-exec and gives TCC one stable identity. | FuseKit `holder.RuntimePlan`, `holder.DeploymentPlan`, and `holder.Runtime` embedded in each consumer's fixed signed app. |
| `one-product-humble-donut.md` | One consumer-owned fixed signed app hosts the broker, runtime, and disposable child roles instead of a standalone Fusekit app or unsigned account daemon. | daemonkit signed-launch/lifecycle primitives, Fusekit runtime/deployment plan contracts and `holder.RunChild`, plus consumer packaging around embedded `holder.Runtime`. |
| `daemonkit-is-too-you-hazy-spark.md` | daemonkit-owned framing may be version-free while each consumer retains one exact opaque product protocol and explicit eviction policy. | daemonkit v0.2 plus exact Fusekit v1 catalog, mount, and source-driver rejection before mutation. |
| `we-broke-something-with-eager-sphinx.md` | Upgrade skew detection and post-install restart policy are generic lifecycle concerns. | daemonkit and consumer release wiring only; no Fusekit filesystem retry or fallback. |
| `would-things-be-simpler-more-precious-sunbeam.md` | XPC does not simplify the filesystem control plane; launchd lifecycle and one persistent trusted session solve the actual ownership problem. | daemonkit persistent Unix sessions and signed-app lifecycle, with Fusekit-owned catalog messages. |
| `its-asking-me-for-scalable-origami.md`, `approve-the-group-container-curious-sketch.md`, and `make-sure-our-fix-cached-valiant.md` | TCC durability follows stable signed bundle, Team ID, App Group, designated-requirement identity, and a stable installed launch target. | daemonkit trust and launch policy plus consumer-owned app packaging bound by Fusekit's holder plan. |

### Implemented or superseded

| Plan family | Disposition |
|---|---|
| `so-the-transient-steele.md` | Holder v2, leases, owner isolation, and carcass proof shipped in v1.0.x. Preserve its safety invariants; replace its self-retire and custom journal machinery. |
| `wise-snuggling-karp.md` | The immediate fork-storm containment shipped. The architectural half is now this rewrite. |
| `warning-couldn-t-sync-federated-kurzweil.md` and agent arms | Version-skew convergence patches shipped and were then superseded by daemonkit takeover/drain. |
| `users-yasyf-cc-pool-accounts-acct-12-pl-eager-map.md`, `ccp-add-01-20-indexed-thompson.md`, and File Provider rollout plans | The original rollout and bounded readiness fixes shipped. Their timeout-oriented and consumer-owned control paths do not survive. |
| `why-is-the-fskit-mellow-tide.md`, `our-claud-epool-is-cached-pillow.md`, `i-thought-we-were-sorted-corbato.md` | Backend selection, FSKit support, and single-mount mux shipped. The rewrite retains supported behavior only where it follows the new source and generation contracts. |
| `the-supervise-adoption-eager-hennessy.md` | Its generic keep-alive arbitration is a daemonkit and cc-orchestrate concern. Fusekit remains the driving `supervise.Pool` consumer and keeps semantic scheduling in its tenant runtime. |
| `lets-do-the-remaning-goofy-duckling.md` | Its Track FD, frozen-retire-wire, self-retirement, and local migration work are superseded. The exclusive-fence, exact-drain, signed-trust, and kill/reap invariants survive in daemonkit plus the new tenant runtime. |
| `is-everythign-downstream-moved-glistening-hollerith.md` and `we-want-to-extracct-cozy-ocean.md` | Their migration inventories and early daemonkit release sequencing are historical inputs to the current release ledger; stale repo statuses and pre-v0.2 surface assumptions are not requirements. |
| `two-loose-ends-crispy-lamport.md` | The useful test-harness salvage landed and the `f3` workspace disposal completed; no holder-v2 implementation is restored. |

### Explicitly reject

- the compatibility proxy and mixed-generation wire design in
  `now-that-we-have-golden-bunny.md`; daemonkit performs a hard generation
  transition, and every consumer upgrades together;
- stable executable copying, including `Spawn.RefreshStable`, stable consumer
  directories, or resurrecting `ReexecStable`; signed installed apps are the
  only executable identity;
- direct daemon reads through File Provider domains, widget-only freshness
  protocols, or cc-pool-owned enumeration and identity recovery;
- longer operation deadlines as a readiness fix. Deadlines bound work but do
  not establish identity, freshness, or materialization;
- automatic fallback to symlink or another backend after a contract failure.
  Selection may choose an explicitly configured healthy backend, but it does
  not hide corruption by switching implementations;
- mass-kill watchdogs, periodic domain bouncing, and retry loops that do not
  carry an expected revision and a bounded attempt budget.

## `fusekit-f3` disposition

The `f3` jj workspace pointed at working copy `263449a2`, whose parent was
`a0eefbab`. The parent is already an ancestor of `main`; it was not unmerged
source. Its only idea was exporting stable-copy refresh for private self-owning
holders. The later signed-app and daemonkit hard cuts deliberately removed that
mechanism.

The f3 working copy changed only generated guidance and configuration:
`.claude/capt-hook.toml`, `.claude/hooks/packs.toml`, `.claude/settings.json`,
`.mcp.json`, `AGENTS.md`, and `CLAUDE.md`. It contained no unique source, tests,
fixtures, or design artifacts. Those same guide migrations already exist on
current `main`.

Disposition completed on 2026-07-19 and was reverified on 2026-07-20. The audit removed
`/Users/yasyf/Code/fusekit-f3` from Watchman, ran `jj workspace forget f3`, and
moved the checkout to the recoverable Trash path
`/Users/yasyf/.Trash/fusekit-f3-20260719-archive`. Verification found no jj
workspace, Watchman root, checkout, or symlink reference under
`/Users/yasyf/Code`. Canonical history still proves `a0eefbab` is an ancestor of
`main`; the archived working-copy delta remains generated guidance and
configuration only.

## Immediate recovery baseline

The Phase 0 recovery on `yasyf-home` restarted cc-pool v0.59.0 only through
Homebrew. The old daemon exited cleanly, repeated account-18 catch-up completed
in under 150 ms, the supported `CCP_ACCOUNT=18 ccp run --version` path exited in
140 ms, and the final state had no account lock holder, lease holder, active
session, deferred apply, or deadline error.

The raw `ccp select` SSH probe exposed another ownership bug: session-leader
discovery associated the lease agent with the root `tailscaled` process, so the
lease could not expire with the SSH command. The exact test lease agent also
ignored TERM and required identity-gated KILL. The rewrite must bind a session
lease to a consumer identity that proves direct liveness and ownership; an
unrelated ancestor or session leader is invalid. Lease termination must use a
bounded TERM/KILL/reap ladder, and restart recovery must discard advisory slots
whose recorded child identity no longer exists.

## Verification gates

The implementation is complete only when all of these hold:

- every repository uses its test wrapper, never bare `go test`; the FuseKit
  gate covers pure, race, fuse-tag compilation, Swift, and isolated-VM suites;
- protocol golden tests prove one schema across Go and Swift, with regeneration
  required to be a no-op;
- crash injection at each drain transition proves single ownership and exact
  worker settlement; no legacy carcass or force path exists;
- signed-peer tests reject wrong Team IDs, identifiers, unsigned peers,
  injection-permissive peers, and stale process identity;
- File Provider mutation tests prove stable IDs through atomic write, rename,
  private-to-computed transition, delete/recreate, and materialization;
- delta enumeration and coalescing tests prove unchanged roots trigger no
  bridge reads and a burst converges to the latest revision;
- the signed-child VM suite proves stable attributes, blocked AppleDouble
  behavior, exact child reaping, and no force-unmount path;
- TCC validation across two consecutive upgrades produces no new consent row or
  prompt for the holder or cc-pool daemon identities;
- the yasyf-home `acct-18 content catch-up: context deadline exceeded` path is
  reproduced before the cut and eliminated after it without increasing a
  timeout. Selection observes typed claim ownership and content revision, waits
  only on a cancellable operation, and either completes or returns the precise
  blocking owner/state;
- the reported 14-domain File Provider workload produces one bounded
  invalidation per changed domain, stable document IDs, zero collision/ESTALE
  amplification, and no delayed-continuation launch storm.

History and plan classifications are evidence, not compatibility commitments.
If an old behavior cannot be expressed by the new ownership, identity,
revision, and generation contracts, it is deleted.
